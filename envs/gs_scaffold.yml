name: gs_scaffold
channels:
  - defaults
  - nvidia
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy
      - plyfile
      - tqdm
      - opencv-python
      - einops
      - wandb
      - lpips
      - laspy
      - scipy
      - jaxtyping
      - typeguard
      - wadler-lindig
      - colorama
